<h2 class="page-header">id <small>model.id( [ new value ] )</small></h2>

An observable property, by default set to `false`, that stores the model's [idAttribute](#idAttributemodel.idAttribute) value.